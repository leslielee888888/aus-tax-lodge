/**
 * Placeholder for the deterministic assessment estimate (PRD FR-8 – FR-12,
 * FR-23, FR-24). The real, no-LLM calculation engine and its golden-set tests
 * land in T3 / T4 / T5 — this stub only fixes the entry point.
 */
export function estimate(): never {
  throw new Error(
    "estimate() is not implemented — the deterministic calculation engine lands in T3/T4",
  );
}
